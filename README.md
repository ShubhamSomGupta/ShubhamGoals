# Shubham Goals — Manager View

Read-only web view of published goals and reports. The desktop application is
the source of truth; this repository contains only the static manager website,
its reviewed publication snapshot, and the GitHub Pages deployment workflow.

The site is built from `manager-web/` and deployed automatically when `main`
changes.
